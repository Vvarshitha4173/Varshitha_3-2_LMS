<<<<<<< HEAD
GIT  vs GIT HUB
Git ---> open-source, locally-installed version control system (VCS)
Git Hub ---> web-based cloud platform that hosts Git repositories

GIT HUB :
FIRST CLASS:
	git init -->(for new repository)
	git clone --> (for dowloading the created respository using hhtps link,ssh)
	we can see the downloaded file in the zip file
	git status --> (helped to check it is changed or not)
	if it shows modified then changes are done(color red)
	git add . -->(changed files can added by using this command,if it shows green color then the files are added which are changed)
	git commit -m
	git merge
	git push
	git pull
	git checkout branch_name
GIT:
open cmd in file location 
>git init
>git add .
>git commit -m "first commit or commit name"
>git config --global user.email "you@example.com"
>git config --global user.name "Your Name"  -->here open git cmd there you'll find your git name , copy it and paste it
>git commit -m "first commit" -->here your project file will be stored in git 
>git remote add origin https://github.com/Vvarshitha4173/Varshitha_3-2_LMS.git -->here open github , login into your account and create a new repository , there you'll find this command and copy it  paset it in cmd
>git branch --> it gives a branch name like master or main if it dosen't give any branch name give the main as branch name
>git branch -M "branch_name"
>git push -u origin master -->here you'll get directly or it will show a window like sign in with your account and click on authroize , now open github refresh it
>github can't able to deploy the dyanmic webpages
>it only works on static webpage deploment
>In github-->settings-->pages-->branch-->master-->save it

19-12-25
AI:
>think like humans , decision making like humans ,thinking , learning...
>which is comdination of DATA+ ALOGRITHMS
>alogrithm-->set of instructions
>types of AI:
	Narrow AI--->weak ai Ex: work only on one specific task
	AGI--->human-level intelligence and reasoning ability
	Super AI--->beyond human intelligence
>AI was generated by using the resources avalible in the wesites before the generative AI
>pervious information - pervious output

GAN AI:
>using pervious information generating new things 

ML:
>is a subset of AI 
>that enables machines to learn from data

PYTHON:
>Tuple , collection of items if it is single line no need of () ,if mutplie line need of ()
>tuple is imputable , ex: GPS coordinates
>List, collection of items , is a mutable , ex: shopping carts
>pull request

20-12-25
JSON Methods:
>json.dump()
>json.dumps()
>pip install virtualenv--->used to create a separate Python environment for a project. and to store the packages which are installed used to different project 
>virtualenv env --> if it is not found
>edit enviroment -->  check the python path in system variables and user variables--> if not , path--> new--> add it -->them click ok 
>virtualenv envFolderName or python -m virtualenv envFloderName
>.\project\Scripts\activate --> to activate the env
>pip install requests--> 3rd party library and used to comunicate to the backend . A Python library used to send HTTP requests
>pip freeze--> to view all installed Python packages
>pip freeze > requirements.txt -->to create a text file 

22-12-25
>create API Ninjas account
>add .env, .gitignore files directly in vscode 
>add templates floder -->html files should be added them 			
>pip install flask--> it is an framework adn a 3rd party library
>pip install dotenv-->.env file ni direct import cheylem and to load the env file dotenv is used
> (__name__) --> which is used to define the main class , is defined by using double under scores 
>objects are created to access them and to store the values of libraries
>main class object should be same as the file name
>import os--> to access the APIKEY value from .env file
>
23-12-25
>to call html files to flask --> render_templates is used 
>jinja-->is used to dynamic template engine {{}} these are called as jinja codes
>requests is an 3rd party library which was developed by companies

24-12-25
>git init 
>git add . --> which is used to add all files in your local computer
>git commit -m "Added new LMS files" -->which helps to upload the new files to git
>to update the contents in the files >git add file_name
>git commit -m "Updated file content"--> this commits the updated content to git
>git push origin master
>to check the status in git >git status
>after this refreash your github then youll get your new files and new contents 
